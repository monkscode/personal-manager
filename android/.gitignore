gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your RELEASE keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: a shared DEBUG keystore (well-known password) is committed on
# purpose so every build has one stable SHA-1 for Google OAuth. Not a secret.
!app/debug.keystore
